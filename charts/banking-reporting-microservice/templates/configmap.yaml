apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "banking-reporting-microservice.fullname" . }}
data:
{{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
{{- end }}
