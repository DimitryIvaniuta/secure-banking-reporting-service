apiVersion: v1
kind: Service
metadata:
  name: {{ include "banking-reporting-microservice.fullname" . }}
spec:
  selector:
    app: {{ include "banking-reporting-microservice.fullname" . }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
